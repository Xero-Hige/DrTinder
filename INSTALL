Cosas necesarias para probar rocksdb:

Antes que nada, hay que inicializar el submodule de rocksdb y jsoncpp, estando en la carpeta del proyecto, con:

git submodule init
git submodule update


Paquetes a instalar:
libgflags-dev
libsnappy-dev
libbz2-dev
zlib1g-dev
python
openssl
libssl-dev
sudo apt-get install 
sudo apt-get install libcurl4-openssl-dev
sudo apt-get install autoconf
sudo apt-get install libtool
sudo apt-get install libboost-all-dev

Instalar rocksdb:
Pararse en la carpeta DrTinder/source/libs/rocksdb

Ejecutar:

make install

Añadir la librería al proyecto (/usr/local/lib/librocksdb.a)

Instalar googletest:
Pararse en la carpeta DrTinder/source/libs/googletest
Ejecutar:
cmake .
sudo make install

Instalar cpprest:
Pararse en la carpeta DrTinder/source/libs/restclient-cpp

./autogen.sh
./configure
sudo make install

Instalar lcov:
Para ver el html de los tests hay que instalar lcov(submodule)
Pararse en DrTinder/source/libs/lcov
sudo make install]

Para agregar jsoncpp, en la carpeta DrTinder/source/libs/jsoncpp ejecutar:

python amalgamate.py

instalar swiften:
Pararse en la carpeta DrTinder/source/libs/swift
#./BuildTools/InstallSwiftDependencies.sh
./scons SWIFTEN_INSTALLDIR=/usr/local /usr/local

